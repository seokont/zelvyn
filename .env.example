# Gmail account used to send early-access requests.
GMAIL_USER=your-account@gmail.com

# Google App Password for the Gmail account above (never commit a real value).
GMAIL_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Optional recipient. Defaults to GMAIL_USER when omitted.
CONTACT_TO=your-inbox@example.com
